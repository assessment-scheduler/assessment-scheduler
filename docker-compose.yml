# Use postgres/example user/password credentials
version: '3.9'

services:
  db:
    image: postgres:latest
    restart: always
    shm_size: 128mb # set shared memory limit when using docker-compose
    environment:
      POSTGRES_PASSWORD: sch3duler567

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080